# Public Assets

Place Maryam's image in this directory as `maryam-image.jpg` or update the image path in `src/components/Hero.jsx`.

Supported formats: JPG, PNG, WebP

The image will be displayed in the hero section on the right side. If no image is found, a placeholder with initials will be shown.

